# Build
.gradle/
.gradle-home/
.kotlin/
build/
app/build/

# Local tooling (machine-specific, project-local — not for version control)
local.properties
/jdk/
/android-sdk/
/gradle-dist/

# Signing secrets (Play Store / release builds — never commit, see RUNBOOK §7)
*.jks
*.keystore
keystore.properties

# Emulator / QA artifacts (machine-local)
.avd/
qa-*.png

# IDE
.idea/
*.iml
.DS_Store
